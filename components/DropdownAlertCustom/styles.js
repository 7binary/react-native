import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  leftIcon: {
    fontSize: 32,
    marginTop: 2,
    marginRight: 2,
    color: 'white',
  },
  closeIcon: {
    fontSize: 32,
    color: 'white',
    alignSelf: 'flex-end',
  },
});